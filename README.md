Build image: 
    ```bash
    docker compose build --no-cache
    ```


Close container:
    ```bash
    docker compose down
    ```


Start docker container:
    ```bash
    docker compose up -d
    ```


